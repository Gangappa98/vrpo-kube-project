apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pv-claim
  lables:
    app: vprodb
spec:
  resources:
    requests:
      storage: 3Gi
  storageClassName: default    
  accessModes:
    - ReadWriteOnce

